# CLAUDE.md

이 저장소에서 작업할 때 먼저 읽는다.

## 이 저장소는 무엇인가

GTD(Global Terrorism Database) 기반 예측 분석 프로젝트. **성격이 다른 두 단계**가 한 저장소에 있고, 섞으면 안 된다.

| 단계 | 시기 | 내용 | 상태 |
|---|---|---|---|
| 1단계 | 2026-04 | 북경이공대 수업 실습. 좌표만으로 무기 유형 분류(KNN) | 종료 |
| 2단계 | 2026-09 ~ | 규칙을 직접 심은 시뮬레이터를 만들어 "어떤 데이터가 있어야 예측이 되는가"를 측정 | 진행 중 |

1단계는 끝난 기록이므로 **노트북 코드를 고치지 않는다.** 개선안이 있으면 개발일지에 적고 2단계에서 반영한다.

## 먼저 읽을 문서

작업 성격에 따라 필요한 것만 읽는다. 전부 읽을 필요 없다.

- **2단계 작업** → `docs/phase2/SPEC.md` (구현 명세, 가장 중요) → `docs/phase2/PLAN.md` (왜 이 설계인지)
- **맥락 파악** → `docs/phase2/DEVLOG.md` (최근 판단과 이유)
- **1단계 관련 질문** → `docs/phase1/DEVLOG.md`
- `README.md`는 외부 공개용이라 내부 작업 지침이 없다

## 핵심 개념 — 이걸 모르면 작업이 어긋난다

2단계의 전부는 **"정답을 내가 알고 있는 데이터를 만든다"**는 데 있다.

시뮬레이터가 파일 두 개를 뱉는다.

- `data/events.csv` — 모델이 보는 것. 사건 기록만
- `data/ground_truth.json` — 정답지. 지휘부 명령, 각 사건을 만든 규칙, 시드

**모델링 코드가 `ground_truth.json`을 읽으면 프로젝트 전체가 무의미해진다.** 채점 함수만 이 파일에 접근한다. 특성 목록에 정답지 유래 값이 섞이지 않았는지 항상 확인할 것.

## 작업 방식

### 커밋

- **문서 초안은 최종본이 나왔을 때만 커밋한다.** 수정 라운드마다 커밋하면 같은 문서를 고친 커밋이 히스토리에 줄줄이 남는다 (실제로 하루에 4개 쌓인 적 있음)
- 코드 변경(버그 수정 등)은 기존대로 의미 단위로 커밋 제안해도 된다
- push 전에 원격과 비교한다 (`git fetch` 후 `git log origin/main -1`)

### 프로토타입 확인

```bash
python -m http.server 8778 --bind 127.0.0.1 --directory phase2/prototype
```

`.claude/launch.json`에 `prototype`으로 등록되어 있다.

**`file://`로 열면 JS가 실행되지 않는다.** Claude Code의 preview 창은 로컬 파일을 정적 스냅샷으로 처리한다(폴더 위치와 무관). 반드시 HTTP로 띄운다.

### 점검은 숫자 먼저, 화면은 필요할 때만

스크린샷 한 장이 수천 토큰, JSON 응답은 수십 토큰이다. 그리고 **두 방식이 잡는 버그가 다르다.**

- **숫자로 잡히는 것** — 축척, 캔버스 크기, 이벤트 수, 심어둔 신호의 실제 크기, 콘솔 에러
- **눈으로만 잡히는 것** — 대비, 밀도, 배치, 가독성

`javascript_tool`로 상태를 JSON으로 뽑아보는 걸 기본으로 하고, 시각적 판단이 필요할 때만 캡처한다. 실제로 검수 기준 오류를 JSON 점검 100토큰으로 잡은 적이 있다.

### 화면 캡처를 파일로 남기려면

`save_to_disk` 파라미터는 **이 환경에서 파일을 만들지 않는다**(전체 프로필 검색으로 확인). 대신:

1. POST를 받아 디스크에 쓰는 작은 Python 서버를 띄운다
2. 페이지에서 `html2canvas`(cdnjs)로 렌더 → `fetch('/save', {method:'POST', body: blob})`

`docs/screenshots/`에 저장한다.

## 이 프로젝트에서 실제로 겪은 함정

- **iframe 초기 크기가 0이다.** 아티팩트 안에서는 스크립트 실행 시점에 요소 크기가 거의 0이라, 그때 계산한 값을 굳혀두면 안 된다. 지도가 0.6px/deg로 고정돼 세계지도가 화면 중앙에 손톱만하게 나온 적 있다. `ResizeObserver`로 관찰하고, 맞춤 배율은 **1회 계산값이 아니라 최소 한계로 취급**해 크기가 바뀔 때마다 다시 적용한다. `window.resize`는 이 상황에서 안 온다.
- **`<meta charset="utf-8">`이 없으면 독립 실행 시 깨진다.** 아티팩트는 래퍼가 charset을 넣어주므로 거기서는 멀쩡해 보인다. 로컬 서버로 열었을 때 `·`가 `Â·`로 나와서 발견했다. 명세가 "디스크에서 직접 열어도 동작"을 요구하므로 파일 자체에 넣는다.
- **위경도에 유클리드 거리를 쓰지 않는다.** 1단계에서 저지른 실수다. haversine을 쓰고, KNN에 넣을 때는 스케일링한다.
- **사건 배치에서 `sqrt(rng())`를 빼먹지 않는다.** 거리를 균등분포로 뽑으면 중심부에 몰려서, 아무도 심지 않은 방사형 밀도 구배가 생기고 분석이 그걸 "발견"한다.
- **기본 지도를 매 프레임 다시 그리지 않는다.** 지형 도트는 lat/lon 공간에 한 번 계산해 캐시하고, 투영만 다시 한다.

## 명세서에 숫자를 쓸 때

`SPEC.md`는 다른 AI가 읽고 구현하는 문서다. 그래서:

- **숫자는 제안이 아니라 결정으로 쓴다.** "적당한 반경" 같은 표현을 남기면 구현자가 설계까지 대신하게 된다
- **검수 기준은 유도하거나, 동작하는 것에 대고 측정해서 쓴다. 추측하지 않는다.** "이동량 7° 이상"이라고 적었다가 올바른 구현이 6.64°로 실패한 적이 있다. 산술을 안 해본 탓이었고, 그대로 뒀으면 구현자가 없는 버그를 쫓거나 기준에 맞추려고 시뮬레이션을 왜곡했을 것이다
- **구현자가 빠질 함정을 미리 적는다.** 위 "함정" 항목들이 명세서에 들어가 있는 이유다

## 문서 언어

- `README.md` — 한국어 (포트폴리오 공개용)
- `docs/**` — 영문 (1단계 노트북 주석이 영문이라 원본 작업 언어와 맞추고, 구현하는 AI에게 넘기기도 좋다)
- 개발일지는 실수와 막힌 지점을 그대로 남긴다. 결과만 정리하면 왜 그렇게 됐는지가 사라진다

## 데이터와 표현

- 원본 GTD 데이터(`terror/globalterrorism.csv`)는 용량 문제로 저장소에 없다. 필요하면 START 센터에 신청해 받는다
- 2단계에 등장하는 조직·기관·사건은 **전부 가공**이다. 실존 단체를 본뜨지 않는다
- 좌표는 실제 지구를 쓰지만 **지형 조건만 보고 고른 것**이며, 해당 지역에 대한 어떤 주장도 아니다
- 합성 데이터로 실제 테러 양상을 주장하지 않는다. 이 프로젝트의 결과는 **방법론에 대한 것**이지 세계에 대한 것이 아니다
